# r7集成验证参考（待#12明确放行）

构建nightwatch-s2-integrated-r7。本文是随包能力与验收说明，不是独立开局授权。先重新读取固定#12正文/updatedAt，只有其中明确给出本构建完整SHA并允许部署时才能执行；r6补证尚未收口时仅做原局分析，不自行切版。禁止覆盖本地改动、reset、改源码/依赖/策略或制造故障。

## 部署与场次

按#12给出的完整SHA取版本，核对HEAD与工作区差异、打包文件清单、包SHA256、实际上传/启用包及首个可取得日志buildId；完整40位SHA和64位hash不得缩写，包排除缓存、.DS_Store和凭据。实际buildId必须为nightwatch-s2-integrated-r7；不符立即停止，不继续开场。buildId字符串不能代替完整关联链。

建议获放行后最多2场（challenger优先、defender其次），实际场次数由#12最终正文决定；错版、协议异常、持续无有效动作或明显预算/回防退化停止下一场。自然未覆盖标未覆盖，不刷场、不制造道具/题目/需求。按PK/阵营/对手/日志范围独立归档，报告真实终局，不把胜局或提前结束等同S2通过。

## #14 重点：T60证据与任务

新增task_detail.inputAssociation、acceptedToolInputs和decision.taskToolInputs用于当前task/request输入归属；区分llmResp建议、executeCmd发出、lastCmdResult回注、下一prompt可见内容、submit实际反馈。长非法信封纠正保留头尾，工具长输出提示精确省略；中段仍可能被省略，不能声称恢复全部答案。外层格式纠正不提高语义正确性，不能以exit0/check字符串/动作成功当裁判验收。

每类自然失败给一个脱敏链、成功给自然对照：错误是否可见、有无截断、实际修正与重跑、剩余期限/工具预算、提交和奖励。API只与同实例文档或有明确共享依据的文档对照，分别记录进程exit与真实HTTP状态；不猜端点或自动拼答案。final-only、迟到/重放/换任务归属边界保持。真实题面、答案/TOKEN和认证不得上传。

## #13/#15：T46采购

仅夜间、存在现有近处紧急威胁/无可发常规攻击、无已有应急库存、商店旁合法闲置角色、容量与现款满足、保留缺塔资金且关键动作优先时可买一件Bomb或Dizzy。每队每夜最多一次实际下单（失败也占本夜尝试），最后夜间回合不买；不远行、不卖矿筹资、不囤货。Bomb须有可击杀目标，否则评估Dizzy控制。

验证buy→实际金币/库存到账→按实时敌情use；不能同轮买即用，失败/未到账不能重复买，威胁消失不强制用，不抢自救/有效攻击/任务/回防。诊断kind=emergencyPurchase有费用/收益计数。无自然触发只标未覆盖，不能据此称故障；不能推断本策略一定能救r6残局。

## T48仅观察诊断

宝藏仍actionEnabled=false、pending_validation。procurement给缺品费用/现款覆盖/容量；这不是可支出预算。route仅对已有完整用品的白天候选评估合法邻接站位、窗口等待、一轮操作和回本请求炮位+两轮余量；最多4候选共享256扩展和原deadline内10ms，截断/缺岗位必须未知。缺品采购绕路及语义可信未验证，不采购/移动/献祭。摘要不含配方/坐标/引文，新闻原文留内网。记录自然候选状态和额外耗时，不把静态路线可行当后续安全保证。

## r6能力回归与反馈

继续检查三塔建设/首夜到岗、多日基地/墙/角色HP、L1/L2真实射程/冷却/有效目标、经济到账及路径预算。按attack条数/目标数/有效观察窗口统计，不把截断16条当完整波次，不把R391白天自动清除当提前清场。

反馈#13经济性能、#14任务、#15防守，每类只追加本批短增量，区分已核实/推导/证据不足/未覆盖。身份统一#15其余引用；decide/serverWriteComplete分昼夜p50/p95/max与>3800/>5000ms，缺字段标不可得。不编辑/关闭#12或缺陷，不自动进入下一版本。
