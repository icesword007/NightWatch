# S2 integrated-r2：本轮内网执行提示

## 版本与运行

仓库icesword007/NightWatch，main，构建nightwatch-s2-integrated-r2。先读AGENTS.md与本文，通用手册仅补充启动/日志规范，旧轮次提示不再执行。检查本地修改后git pull --ff-only origin main，不覆盖本地变更、不reset。实际完整HEAD必须与外网本次提示的送测SHA一致；包含db579b28616a87f0129fd3b1b7f3cbcc0bf60d99和4f6eb5520a7403eb287abd598dc02683be63250b两祖先还不够，必须同时含本次任务/波次修复。记录SHA、build、包SHA256、Python版本、包文件清单及本地差异；不只看build。

上传CoreGeek运行文件，保持main3.py、run.sh、src/agent结构，排除.git/.DS_Store/__pycache__/*.pyc/环境文件；不删除本地缓存。不得自行改源码、依赖、参数或注入道具/新闻来制造覆盖。完整题面、日志、答案与凭据留内网，issue只发脱敏证据摘要。

先跑一场：优先能持续到第三夜附近、同时有任务和投资机会的对手，报告实际阵营，避免过弱对手过早结束。只在明确另一阵营或关键机制可带来新证据时建议补测，不自行刷局。本轮不要求等十天才能反馈；胜利/对方先毁不等于完整第三夜通过。

## 优先观察

### P0 任务入口修复及真正求解（#10）

实际“请阅读xxx.md，获取任务信息”应走确定性有界读取，核对第一响应executeCmd是否python3 -c、其结果和首次正文可用回合；taskEntryReadAttempted不是命令已发出/读取成功。记录安全脱敏题面语法（repr保留空白）、cwd与任务根的相对关系、TASK_INPUT_STATUS、路径位置类别及截断标记。无自动命令或失败时判断正则、根目录、边界/歧义或结果回注，不把LLM裸cat归因自动reader。

至少追一条从接取到结算的完整链：每轮prompt/LLM返回/命令/结果/预算/答案契约/裁判反馈；GLM 5.2根据完整内容指出第一处可证偏差、最短可行解及剩余瓶颈。入口更早读到不保证完整答案正确。按实例统计接取、提交、完整成功、部分奖励、失败及实际金币/积分；code=2不等于零收益。旧PK614447 R25有+16部分奖励，旧#10“零部分奖励”统计错误应更正，避免延续冲突。

### P0 波次累计与预测（#11）

按源码当前字段确认诊断含义，不预设predicted字段。新字段basisCompleteness/finalized说明预测依据与定稿状态，actualCompleteness/actualFinalized说明实际累计与定稿状态；只有整夜60回合连续观测且收到次日天亮回合、无ID截断才标完整。按targetDay/basisDays关联forecast/history/currentNight：选一夜首次观测、新ID后续新出现、夜末、次日天亮四个节点，记录去重各类累计数/完整性/截断、预测数及实际数/误差。晚到机器人应进入累计与实际评分，下一夜预测不能永远停首帧；原先对该夜作出的预测不可被实际值反写成“预测正确”。缺首帧、缺轮、中途结束或容量截断不应标整夜完整。不把普通turn机器人列表截断与压力统计ID上限混同。

官方规则机器人全图可见（普通单位4格视野限制不适用机器人）；后续新增ID与日志截断分别分析，不把它们一概解释成视野外来敌。首次未观测类型应表达未知/不确定，不能要求准确猜出数量或硬编码第三夜大型3只。若自然没有后续新ID，更新场景记未覆盖，不注入数据。R331–390是第三夜，R391天亮；逐场给实际末轮/终止原因/双方基地状态。

### P1 已持应急道具（#11，独立根因可新issue）

本批只使用已有Bomb/DizzyWeapon，不采购。先查真实背包来源和存量，无库存记未覆盖，不判功能失败。自然触发时，核对夜间、基地附近3格紧急我方目标、当轮无普通武器attack、持有者无任务/自救/资金持券占用；每队每轮最多一次use，不移动原炮手。Bomb需可击杀至少一个HP≤100紧急目标，否则才考虑Dizzy。列use前后库存、反馈、敌人HP/眩晕、己方基地HP、下一次炮手开火；同目标明确失败不无限重复，已眩晕不反复控制。区分动作合法/效果生效/生存收益，单场不能证明因果收益。

### P1 宝藏结构化条件只读评估（可新建独立issue）

只在既有新闻普通额度内分析，自然候选未出现记未覆盖。核对地点/绝对时间/用品逐字段引用及顶层来源来自实际发送文本，session与截断标记正确；让GLM核查引用语义，不把引用存在等同配方真实。部分条件后续补全应相容，用品顺序无关且数量敏感。localChecksPassed可真但status恒pending_validation、actionEnabled恒false；不得出现由宝藏导致的购买/移动/summonTreasure。turn不含用品名称/原文，详细引用留news_detail；任务仍优先、新闻额度不增加。

### P2 经济、防线与回归（#9/#11）

用真实价格闭合初始+出售+任务到账-建造/购买=余额。资金链售出、到账、购买、持有、使用、等级/HP变化和回岗分开。墙券门控开放不代表必买；若再认为漏买，给现金+可售库存/价格、有效承诺、位置/路线/余时、其他候选和实际选择，不凭investmentTarget单字段判门控失败。不主张无证据中断修墙；单买方不重复不能验收多人协同。

基地首次失血和致命突破前后给逐轮墙缺口、炮手HP/控制者、三塔等级/冷却、实际attack/落点与反馈。冷却0时是否正常攻击，不能把稀疏摘要写成连续十轮无攻击。三火箭算法未调，未验证完整敌人列表时不能断言打空。协议异常/超时/重复动作列真实统计，未提供的指标写未知。

## 反馈与停止

#10任务、#9经济、#11防守/波次追加短增量；宝藏等独立根因新建issue并关联版本。正文约2000字、单评论约1500字，避免复制旧全文。各项标通过/失败/未覆盖/证据不足，给完整SHA、PK/阵营/回合、脱敏事实、分析及复测条件；纠正旧误判时明确撤回，不让新旧矛盾并列成为当前结论。不自行关闭issue。

启动/协议故障立即停止继续对战；稳定同一失败且无新增证据时停止。最终给一页覆盖矩阵、最值得修的1–3项及是否值得补另一阵营。记录上传→对战→日志分析→issue回传耗时，未知明确标记。
