# NightWatch S1候选版内网验证手册

日期：2026-09-13。适用于构建标识nightwatch-s1-r5。实际版本以参赛提交SHA和包内容为准，构建字符串不能替代SHA。

## 1. 本次验证目标与职责

本版保留S0/B的HTTP、状态、约束、寻路和重试保护，包含C批经济与首夜防守，并新增D批真实任务闭环：选点、移动、领取、`phaseTask`实例、平台LLM `prompt`、平台任务沙盒`executeCmd`、结果回注和完整/部分答案提交。r5进一步加入按可兑现价值/动作成本选矿，以及正好两工人的真实库存联合筹资、到账确认和不同炮位回岗。不包含主动宝藏、Bomb/DizzyWeapon、机器人召唤或直接进攻。三塔混合、12回合黄昏阈值、一次性早期围墙和任务/防守仲裁都是保守基线，不是最优性结论。

内网Agent负责获取版本、上传、发起对战、分析和GitHub issue反馈；代码由外网开发Agent修复。原始日志不能回传，保留在内网供复查；issue只写允许传出的最小事实摘要。无法披露的字段明确标“受限不可提供”，不要改造成虚构数据。

## 2. 获取版本与启动

1. 阅读仓库AGENTS.md。运行`git status --short`、`git rev-parse HEAD`、`git remote -v`，确认仓库为icesword007/NightWatch。工作区有修改先报告，不覆盖、不reset。
2. 在干净工作区按用户安排拉取目标版本，例如`git pull --ff-only origin main`，再保存完整HEAD SHA。拉取失败不强制覆盖。
3. 记录`python3 --version`。赛方环境说明为Python 3.11.10，本地开发已验证3.13.3；内网须验证实际3.11环境。程序使用标准库，无需pip安装。
4. 候选上传根目录为CoreGeek内容，其中run.sh、main3.py、src/agent/*.py必须保留相对布局；确认平台解包后实际运行位置。若平台要求不同，先报事实和所需调整，不擅自改程序。
5. 启动方式：在CoreGeek目录执行`bash run.sh <平台指定端口>`；监听0.0.0.0，HTTP POST根路径。不要把尖括号占位文本当参数执行。
6. 可以先在CoreGeek执行`PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest discover -s tests -p 'test_*.py' -v`，预期170项通过；本地测试覆盖状态、资源约束、寻路、经济/防守/任务策略、严格工具时序、响应绑定诊断、有限取证日志、协议和HTTP，但不替代对战。
7. 上传仅包含实际运行文件，排除.git、.DS_Store、__pycache__、*.pyc及本地环境文件；无需删除工作区文件。记录打包所依据SHA、包SHA256（工具可用时）、包含文件清单和启动设置。平台如另有构建步骤一并记录。

本次没有内置对局ID字段，也不能仅靠buildId判断代码版本。若上传包有任何未提交源码差异，必须注明“修改版”，列明差异，不能称原SHA已通过。不要在记录中暴露凭据或带token的URL。

## 3. 必测点与结果状态

每点使用“通过 / 失败 / 未覆盖 / 证据不足”，不能把没报错等同通过。

| ID | 观察与通过条件 | 不足时怎么处理 |
|---|---|---|
| V01 | 目标版本在官方环境成功启动，端口和绑定正确 | 启动/导入/打包错误直接报错，不换对手重复撞同一错误 |
| V02 | 连续至少2个真实回合请求获得完整JSON响应；记录观测窗口内裁判侧异常数 | 请求中的`errors=[]`只表示该次请求未携带错误，不等于裁判累计零异常；没有裁判统计就标证据不足 |
| V03 | 至少一次move真实生效：指令角色属于当前存活工人/开拓者，目标相邻且静态可通行，下回合位置与反馈支持移动成功 | 碰撞单次失败不算协议异常；查目标争抢或障碍，改条件补测 |
| V04 | 记录裁判侧响应耗时，确认观测窗口未触发5秒响应超时 | 只有程序日志时标裁判时延不可得；服务端指标不等于端到端时延 |
| V05 | 两个阵营分别核对角色ID、地图方向与移动效果 | 未跑到某阵营标未覆盖，安排换边/其他可用对局 |
| V06 | 有移动指令时，空字符串prompt和executeCmd被裁判接受，无错误调用或额度异常证据 | 若无额度观测，只能确认响应形式被接受，不能证明额度没扣 |
| V07 | 自然出现无可移动角色时，roleCommandMap为空的响应被裁判接受 | 未出现则未覆盖；不改代码制造该场景，不无限重试 |
| V08 | 能关联构建、SHA、对局、阵营、回合、输出动作、受控角色位置、双方基地状态与下回合反馈 | 缺证据时补日志分析能力或平台资料，不猜失败归因 |
| V09 | 至少一名工人真实完成“采集→背包出现→出售→金币增加”；有多个当前可见矿点时，选择应结合正式售价与从当前格到采集、兑现的实际动作成本，明显更高的可兑现价值/回合可替代最近矿；路线或报价变化后重评，采集失败或矿点消失后不无限重试 | 分步记录目标矿种/坐标、`decision.actions[].economy.estimatedValue/estimatedRounds`、工人位置与背包、正式售价、动作和下一回合反馈。每次collect只按1份滚动估计；赛方确认每矿最多10次后消失、下轮随机刷新，但请求不提供剩余次数，因此不得假定当前矿尚有10份，也不得把未采资源计作资金 |
| V10 | 使用当前实价完成单人或两工人“出售确认到账→唯一买方购买→背包出现→使用→两人回到不同炮塔”；联合筹资只在正好两工人、三塔已齐、双方当前真实库存合计可承担且完整双路线赶得上截止时启动，个人可独立承担时仍走单人链路 | 逐轮记录采购目标/使用目标、参与角色、各自当前库存估值、`expectedContribution/currentGold`、两条实际路线、绑定炮塔、动作和反馈。第二笔出售失败或未反馈、角色死亡/被自救占用、涨价、需求消失、路线或截止失效时不得用预计金币购买；核对接受的回退动作上`jointCancelReason`，不得重复消费共享金币或让两人占同一炮位 |
| V11 | 白天建塔仅落在基地蓝环可建格，总数不超过3，同回合金币不超支；缺25金但本人矿值足够且路线及时的第三塔允许“出售到账→到已选建造位→build”，建造失败后不无限重发同一格 | 记录矿价、出售与金币到账、锁定建造位、实际路线、build反馈；优先核对蓝环坐标和失败码，配图推导与平台不一致建“规则待确认”issue |
| V12 | 有三塔、白天且距入夜大于12回合、无现存墙且工人带stone时，全队本会话最多启动一个黄环墙实验；同回合多工人、跨回合途中、反馈缺失/失败和原负责人死亡都不得另开第二个 | 围墙价值未证，失败不作为C批主线失败；但重复尝试或抢占关键通路必须报缺陷 |
| V13 | 黄昏前炮手按实际路线到武器一格内；重叠可控范围采用一对一匹配，不因塔顺序少开可覆盖的塔；已到岗或仍有效的既有配对在不降低按时覆盖数时保持；有效资金路线绑定的最终岗位不被另一长期计划占用。三塔必须依赖开拓者守第三岗时，即使当前无目标或塔冷却也不为未领取任务离岗；目标出现时正常攻击，黎明或两塔已由工人覆盖时释放。确有第三座有效武器、两名工人已承担另外两岗且无替代者时，活动任务预算须扣除到第三炮位的回程动作并提前收敛；可靠答案只有在提交后仍能到岗时先提交，提交与move不得同回合并存 | 按回合记录每座武器/控制者ID及Chebyshev距离、各角色长期计划、资金绑定岗位、当前目标/冷却、任务实例、剩余任务轮、预计回程动作、最晚离点与实际到岗回合。距离必须为1才算已到岗；重叠关系需列出全部可选边后核对最终匹配。无合法目标时不应强造attack，可由空命令守岗；只看到攻击不证明按时到岗，只试一场不能概括策略稳定性 |
| V14 | 夜间攻击的武器存活且冷却为0，controller相邻，目标在射程内，目标数符合武器等级，下回合反馈成功；HP≤40且持Medicine的炮手本回合改为原地用药 | 分清协议拒绝、规则不合法、冷却、目标消失和紧急自救；40为策略阈值，不用胜负代替动作证据 |
| V15 | 我方基地缺席/零血样本不再生成基地建造、升级或召回；残存角色/武器仍只发合法动作 | 样本未出现标未覆盖；不为取样修改对局请求 |
| V16 | 开拓者只向本方`isValid=true`且冷却为0的任务点移动，并在相邻一格后发出`acceptTask`；任务2若显示两格，记录实际可领取格 | 记录任务类型、任务点、前后位置、领取动作与下一回合反馈；不能用出现题面倒推裁判反馈为true |
| V17 | 领取后出现非空`phaseTask`，下一回合程序发出非空`prompt`；已知截止时每次prompt都携带当前剩余轮数和是否允许命令探索，返回只接受command/answer/abandon严格JSON信封 | 自包含历史与显式截断已由本地测试覆盖，运行日志不暴露正文；内网仅记录回合、prompt是否非空、信封kind/complete和错误类别，不要求原文 |
| V18 | `kind=command`只在预算允许时作为同回合顶层`executeCmd`交给平台任务沙盒；下一回合`lastCmdResult`再形成新prompt，本机无命令执行；已知只剩1轮时不得再执行命令 | 记录命令请求/结果状态与严格前后回合关系，不回传命令或输出正文；同回合或迟到结果不得串入，截止前应保留答案提交轮次 |
| V19 | `[exitCode:0]`可作为命令成功证据继续求解；空值、非0、`[TIMEOUT]`、`[JUDGER_ERROR]`、`[TRUNCATED]`均不被当答案或任务成功 | 同一命令与结果对连续第三次出现时只再请求一次基于证据的完整/部分答案或abandon；之后仍返回命令/非法响应或证据不足时停止工具循环并只尝试一次合法离点。新题面实例才重置。若平台格式不同，附最小脱敏形态并建规则待确认issue |
| V20 | `kind=answer`形成开拓者`submitAnswer`；`complete=false`可提交可靠部分答案；同一开拓者同回合不会同时提交与移动/攻击 | 记录答案是否完整标记、动作反馈和后续任务状态，不回传答案正文；提交动作成功不等于题目完整通过 |
| V21 | 活动任务开拓者不被普通维护移走，可原地用Medicine；已贴有效塔时可立即开火。远程紧急回援仅在夜间、明确我方威胁距基地不超过3、存在冷却0且能打中该威胁的无人塔、开拓者可赶到而等待一回合即错失、且没有空闲替代者时移动优先 | 记录任务/基地/塔/开拓者/威胁位置、目标阵营、路线回合及连续动作；值守其他塔的炮手不算空闲替代者。距离3为待校准阈值；任一条件不满足不得召回 |
| V22 | 任务结束区分超时、开拓者死亡、离点和未知；至少一类任务取得裁判可核实的完整成功证据，并核对金币/积分时序 | `phaseTask`消失或奖励变化单独都不足以证明完整成功；缺正向结果字段时标证据不足并保留issue |

S0/B回归需启动、连续请求、确认动作效果和可核实的响应/异常证据；C批至少完成V09–V14中实际出现的关键链路；D批必须走通V16–V20，并为V21–V22取得可触发部分的证据。首夜“发出合法攻击”与“基地存活到天亮”是两个独立结果，前者不能替代后者。V04没有裁判侧耗时就必须写证据不足；V12只允许把全队一次墙试验记为一次试验；V13只有在最晚到岗回合前实际抵达并形成有效操控才算按时。条件性场景未触发时单独保留缺口，不将其他观察作废，也不能声称所有边界已通过。

## 4. 如何读程序日志与分析

成功日志为一行JSON摘要：buildId、带时区的timestampUtc、roundNo、team、economy（金币/总分）、controlledRoles（工人/开拓者位置、HP、背包容量及S1白名单库存计数）、ourStructures（基地/塔/墙的位置、HP、等级、冷却）、robots（有限威胁字段）、shops（白名单当前价）、bases、commands、actionFeedback、errorCodes、task、decision和timingMs。task仅含idle/active、任务点元数据、是否请求prompt/command、LLM返回是否非空及合法信封kind/complete、命令结果类别/截断标志和是否提交答案。decision与同次响应在同一决策临界区生成，含回合、内部任务实例、有效剩余预算、solver/离点原因、命令/结果/循环的短哈希，以及实际进入响应的领域、固定类别原因、预计动作回合和截止；被接受的选矿动作可带目标、矿种、预计价值/回合，被接受的联合筹资动作可带采购/使用目标、参与者、预计出售贡献、当前真实金币及撤销原因。缓存重放复用同一响应与诊断快照。

`decision`不含题面、prompt、答案、命令、LLM响应、沙盒输出或新闻正文；经济诊断只位于实际接受动作的`decision.actions[].economy`，没有进入响应的候选不会写日志，撤销原因仅在同角色存在已接受回退经济动作时附加。`coordinationReason`的实际路径是`decision.coordinationReason`，必要炮手阻止新任务时记录`gunner_hold`。短哈希只用于同一对局内关联是否重复，不能还原内容，也不能单独证明命令正确、语义进展或任务成功；多组指纹不同不能称为精确重复循环，success/failed数量必须按实际分类逐项统计。并发HTTP分析必须以各请求局部日志中的roundNo、taskInstanceId、fingerprint与响应动作关联，不从另一个回合推断本回合原因。

所有日志列表使用`items`加`truncated`，每类最多16项；动作目标最多3个，并单列`targetPosTruncated`。背包只计stone/iron/copper、Medicine、WallFixer及Station/Weapon/Wall两级升级券，未知物品不写日志。命令结果只分`none/success/failed/timeout/judger_error/truncated/unknown`。基地不存在时只记录`present=false`，不能解释成零血。commands属于当前回合；actionFeedback来自上回合动作，必须跨回合关联，不能当当前动作结果。

processing为服务端读取解析到编码的时间；serverWriteComplete为服务端写入响应完成的时间，二者均不代表裁判收到响应的耗时。日志在写回响应后输出。buildId当前是固定标签，真实SHA从上传记录提供。

例：回合r的`controlledRoles.items`显示工人W位于(x,y)，`commands.items`输出move到(x+1,y)；回合r+1反馈W=true且角色位置变为(x+1,y)，可认定该次移动生效。若W=false，结合目标位置/其他单位行动辨别碰撞或阻挡；证据不够就列候选原因与缺失观察，不断言程序错误。双方基地的实际路径是`bases.our`与`bases.enemy`，可证明请求中基地是否出现及其当时血量，但不能单独证明最终胜负或终止原因。白天R66–70没有攻击不能记为射击延迟；还须在夜间同时核对射程、目标和冷却。动作反馈false必须先关联到前一回合owner和action；已知两条false是move时，不得改写为attack协议失败，具体失败原因仍需位置与占格证据。

程序摘要可以直接证明：该回合请求里观察到的金币、分数、白名单库存、己方建筑、有限机器人、商店价和任务点；程序输出的动作类型/目标与工具调用形态；下回合请求携带的按ID动作反馈、错误码与工具结果类别。任务点的`scoreReward/goldReward`只是奖励元数据，不是实际到账；奖励必须结合提交前后`economy.gold`与`economy.score`（请求原字段为`teamOur.goldNum/totalScore`）及同轮其他收支判断。连续回合结合位置、库存、金币、HP和反馈，可形成V09–V21的动作链证据，但任何`truncated=true`都表示该类观察不完整。

必须从裁判页面或内网额外证据取得：裁判端到端耗时与累计异常数、对局最终胜负/终止原因、任务答案真实通过率与“完整成功”、奖励变化是否由某次提交导致、平台是否持久化沙盒、以及任务结束结算规则。多步prompt携带历史、固定结果停止重试等内部行为已有本地测试；为避免泄密，运行日志只记形态，内网不需要也不得要求题面、prompt、命令、LLM、输出或答案正文来重复证明。

B批公共基础在本地已有单元与HTTP测试；其中动作/工具结果只关联严格前一回合，同回合替换响应会撤销原待确认记录，锁竞争超出内部预算时返回当前请求独立生成的基本响应且不修改共享状态。平台侧只验证它没有破坏真实协议链路。若平台可安全重放同一请求，可记录同回合同正文响应是否完全一致；若平台不提供这种能力，标未覆盖，不修改裁判请求、不用代理伪造流量。换边后重点确认首次响应正常且日志team与实际阵营一致；内部动态状态是否清空仍以本地测试为主，平台没有状态观测接口时不要虚构证明。内部截止是主动软截止，不能中断已经开始的Python解析或系统调用；仍以裁判侧观测判断5秒限制。

区分四类：启动/程序异常；格式或未知指令导致的参赛异常；字段完整但因碰撞等未生效的游戏动作；规则或证据未知。赛题规定异常累计5次停调，程序异常退出不再拉起；合法指令执行失败不等于队伍异常。程序返回HTTP400/500不是合法回合兜底，应记录触发请求的允许披露字段与错误摘要。

## 5. 选对手、补测与停止

- 首轮选能顺开局、触发至少一类任务并至少运行到R131的常规对手，记录选择原因；若可用优先安排双方阵营。首场主要取证协议、经济闭环、任务闭环和首夜动作；R130前提前结束的场次不覆盖完整首夜，不用强敌局的胜负替代动作合法性验证。
- 对局过早结束、角色被封住、日志不完整：指出缺哪项V证据，选择较低压力对手或另一阵营补测。
- 已能确定的启动/协议缺陷立即建issue并停止无意义重复；对手变化不会修复错误入口。
- 首轮后最多补测2场作为初始成本上限；每场必须针对具体缺口。无新增证据或达到上限就汇报剩余项与建议，等反馈，不无限刷场。后续可依实测成本调整。
- 不以胜负、总分或坚持回合数单独判定S1通过；至少一类真实任务完整成功与双阵营首夜仍需分别取证。对手风格可记录为后续研究线索，不因研究扩展本次必测范围。

## 6. GitHub issue反馈模板

先查已有issue，复测在原issue补评论；独立问题单独建issue，首次总体结果可建一条S1候选版验收记录。标题建议：`[S1][现象] 简短描述`。不要只写“有问题”或“已解决”。

```text
类型：验收记录 / 缺陷 / 规则待确认 / 证据缺口
参赛完整SHA：
构建标识：nightwatch-s1-r5
工作区/上传包差异：无，或逐项说明
包SHA256与启动方式：
Python版本：
对局ID、阵营、对手及可得版本：
地图/种子：可得值，或不可得
观察窗口：回合…至…

逐项结果：V01…V22，每项通过/失败/未覆盖/证据不足
期望：
实际：
最小证据：UTC时间、回合、阵营、角色ID、前位置、输出指令、后位置、双方基地状态、反馈/错误码、任务阶段及工具结果类别；不含题面/命令/输出/答案正文
服务端耗时 / 裁判耗时：分别记录；不可得不要填0
程序异常与动作执行失败：分别统计，注明数据来源
分析：已证实事实 / 推断 / 无法排除的解释
内网证据索引：内部保存位置或代号，仅传允许信息
复现条件及次数：
已补测对手与结果：
仍缺哪些观察，建议如何补测：
验收条件：修复后需看到的明确结果
原issue / 被测修复SHA：
```

首次同时记录上传开始、对战开始/结束、日志可分析、issue提交时间（含时区）、人工耗时，以及可取得的日志粒度、固定种子/批跑能力、排行榜与对手信息。用于估算仿真收益，不虚构缺失时间。

## 7. 修复复测闭环

外网主Agent读取issue→核实问题→开发修复并推送→内网Agent按约定触发复测→原issue补完整被测SHA和验收结果。不是自动定时任务，本手册本身不创建监控。

修复后选能复现问题的对手/条件，并保留基本启动与移动回归。全部对应验收条件有证据通过才关闭原issue；任一未通过继续open。拉取或推送成功、版本号变化、一次对战获胜都不是关闭依据。无法确定如何取证时反馈缺口，外网主Agent决定下一步。
