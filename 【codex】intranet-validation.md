# NightWatch 内网验证手册

当前统一构建`nightwatch-s2-integrated-r4`，已合入P1白天动态回防与P2任务失败证据/重复保护，保留r3任务根优先读取、夜间清场释放与规划性能修复。唯一执行入口为 https://github.com/icesword007/NightWatch/issues/12 ，完整SHA、批次、场次与停止条件以最新正文为准，按批次去重；历史提示仅归档。

白天工人按完整工作链、真实唯一炮位返程及2轮余量决定继续或回岗；路径未知/预算耗尽不能由最终经济候选或任意兜底移动放行。采矿需兼顾完整清算，施工规划保留其他角色精确炮位。任务长输出在原预算内保留头尾；同一命令连续两次明确非零退出后拦截第三次并至多一次纠正，不影响改命令后的重试。工具exit0与日志completed仅指进程完成，不是答案正确。

R195报告的连续约3秒经济规划、检查通过未提交及统计矛盾仍需原局补证，本批不声称已修。执行仓库【codex】s2-integrated-r4-intranet-prompt.md及#12当次范围，不自动补场。

隔离树另有本地候选`nightwatch-s2-procurement-r1`，只对尚未离开当前矿点的新非紧急WallFixer/WallUpgrade资金链比较立即兑现与固定有限追加采集；收益按完整出售、购买、使用和真实回岗总回合计算，额外矿须实际出售。紧急修复、关键升级、已有fund/joint/持券保持立即执行。该候选尚未合入或送测，固定issue #12仍执行r4；不得据此改变当前内网批次或宣称实战收益。

## 0. 当前增量与反馈规范

任务文件名先在/tmp/selfEvolutionTask完整搜索，唯一合法匹配才读取；根不存在或完整无匹配才回退当前目录。同域歧义或扫描不完整仍拒绝，两个域共享原时间与条目预算。显式路径和后续任务求解不变，任务根内部过深、预算耗尽或答案错误不能一概归为本批已修复。

夜间首帧之后，完整全图观测确认没有攻击我方的存活机器人、也无未知目标时，逐轮释放纯炮手，复用现有采集/交易/任务等工作；威胁再现或观测缺失恢复防守。仅对方目标机器人仍作为路径障碍。保持真实昼夜与夜间禁建，保留有效任务/资金承诺，不人为固定留人空守。白天工人现按动态回防窗口；开拓者原任务截止/召回与普通采购批量保持。

保留批量采石、安全通道、低收益维护门控，以及围墙增量安全搜索/内部截止控制。nightwatch-s2-efficiency-r2的PK628365有性能与施工恢复正例，但本次统一构建须新局验证；获胜、单阵营或提前结束不等于S2整体验收通过。

#12只保留当前提示，由主Agent全量更新；内网不得编辑/关闭该issue或在其中堆结果。反馈写对应#9/#10/#11，标批次、实际参赛完整SHA、包hash、对局ID与实际阵营，提供脱敏增量及已核实/推导/证据不足/未覆盖状态。日志、题面、答案和凭据留内网。推送或本地回归通过不代表实战验收，不自动关闭问题。

用户已向赛方确认：基础机器人入夜一批生成；白天使用召唤令，当晚入夜生成增量。夜间后段首次记录新ID不直接等同实际生成，应核对原始逐轮列表。夜间build非法，清场后工作收益不能计入夜间造墙。

以下为历史能力与运行说明，旧版本名称及当轮测试要求只供追溯；执行范围一律以固定issue #12最新正文为准。

## 1. 历史能力演进与职责

**当前r3新增：围墙升级采购闭环。** 仅三座武器已存在、当前session固定蓝图墙目标全部真实建成后，才在原Medicine/WallFixer/Weapon/Station候选之后加入对应等级的WallUpgradeVoucher1/2；商店价格和背包以本轮实际输入为准。已有墙券兑现不受新采购门控阻断，日夜仍遵守原相邻使用/回岗规则；不重算蓝图来绕过缺墙，不覆盖建墙回血。投资目标和多人owner复用r2。

验证必须记录买券成功、物品入本人背包、移动到目标、使用反馈和下一轮目标等级/HP；命令已发不等于效果成功。核对两人不重复购同目标、不超共享资金、满级不再买、缺墙时不启动新墙券采购。该顺序不是未来资金硬储备：高优先投资当前无法兑现且尚无有效承诺时，低价墙券仍可能先成交。墙未齐的对局可能完全不触发此能力，标未覆盖；不称它已修复首日施工差异或第三夜失守。

本版保留S0/B的HTTP、状态、约束、寻路和重试保护，包含C批经济与首夜防守、D批真实任务闭环，以及r5的按可兑现价值/动作成本选矿和两工人真实库存联合筹资。r6将联筹寻路复用限制在单次请求内，设置1600次经济寻路硬上限并只给经济规划75%的请求预算。r7继续检查同一目标的其他候选邻格，不再让单个邻格的局部`expansion_limit`否定已找到的可达路线；经济不缓存这类不完整结果，防守仍在全局deadline到期时立即停止。联合筹资的`jointStatus`可进一步区分返程超期、出售/购买路线不可得、岗位不可得与动作冲突等有界固定原因。该历史阶段不包含主动宝藏、Bomb/DizzyWeapon、机器人召唤或直接进攻，也没有改变投资偏好、LLM回答逻辑、攻击协议、低血撤退或总超时。

r7只为任务求解prompt增加文件定位原则：明确路径直接访问，只有文件名时有界查找，无文件信息时仅在唯一任务相关输入明确时使用，不假定整个沙箱只有一个文件。既有“剩3回合可命令→剩1回合禁止新命令但允许提交”时序保留，没有因本批提高命令阈值。

r8在三塔完成后预留一名稳定工人，按当前地图双方基地实际横坐标形成水平相向的来袭方向先验，主动采stone并施工最多6个前沿/侧翼关键墙格；逐格保留后侧通道、独立炮位和角色可达性，并用真实路线检查采石、建墙与回岗期限。这个方向来自当前地图观察，不是所有地图、波次或召唤路径的永久规则；敌基地缺失时才使用我方基地相对地图中心横坐标的确定回退。白天炮位只在不增加路线成本时偏向塔后侧，夜间既有守岗和攻击顺序不重排。已启动fund链、持券即时兑现、Medicine和任务角色不被施工抢占，另一工人继续普通经济。建造失败只跳过当前格，不把整条防线作废，也不无上限补建。

r8同时新增独立`event=task_detail`任务详细取证日志。它默认只在任务相关请求出现，白名单记录题面、发出prompt、LLM原返回、实际命令、平台结果、实际提交和错误/反馈；每个正文字段最多131072字符，记录原长度、日志截断、平台`[TRUNCATED]`和SHA-256指纹。该日志在HTTP响应写回后输出，不进入裁判响应，也不替代原`event=turn`摘要。真实正文只留内网，issue仍只回传脱敏结论。这些能力不证明围墙免伤、任务已正确求解、590494资金停滞或590568缺岗/基地失血的完整根因，也不证明S1已通过。

r9在延续旧普通`mine:`计划前重新检查当前可完整执行的单人资金链，避免已经足额且路线及时的工人仍被旧采矿目标压住；施工者、持券、已启动资金链、自救与防守保留原优先级。任务prompt明确一次命令到结果回注占两个游戏回合；仅截止未知的任务最多接受4个命令周期，已知截止依剩余预算和重复收敛。每题保留有界的最早两条与最新一条成功证据，并仅从成功沙箱输出提取最多8条环境路径线索；路径在新题仍须验证，换对局清空。真实任务截止剩3回合时不再开启新命令探索，但在剩4回合prompt已请求、到剩3回合才返回的有效命令不会被丢弃；可撤销的防守协调截止消失后不会留下永久收尾锁。施工批次最多2墙，受剩余安全目标、背包容量和真实建墙回岗窗口共同约束；矿消失会缩短批次。暂时占格以有界条件指纹记录：条件不变时不重复昂贵安全搜索，可跨超3回合后恢复；最多2次不同条件变化可重查，第3次不同变化仍阻塞、裁判build失败或永久不安全才进入失败集。

暂时阻塞实际分开两个有界指纹：完整安全校验指纹用于判定是否必须重做安全搜索，实际阻塞依据指纹才决定是否消耗复查次数。因此无关工人移动或HP变化会使旧安全结论失效并重验，但目标格仍由同一角色占用时不增加复查次数；占位者离开后再以当前完整上下文验证，不复用旧安全结论。

r10收紧任务路径线索：仅缓存成功沙箱输出中独立成行的绝对路径，不把路径后的解释文本一起当作路径；`exitCode:0`只是线索来源条件，不等于路径已验证适用当前题。当前题新发现的路径可继续作为当题线索；新题显式给出任一完整路径时，历史仅复用完整一致的路径，不再退回同文件名的其他目录；只给出文件名时，才在历史中复用唯一同名候选。同名歧义或无关新题不复用旧路径，完整路径按边界匹配，不把`/foo`视为`/foobar`。中文等非ASCII独立路径仍可保留。

r10在领取需要开拓者回第三炮位的新任务前，用实际路线校验“到达任务点+首次prompt+命令下发+结果回注+答案提交+回岗”的白天窗口；任务自身截止少于5回合也不领取，因为4回合边界在领取后首次prompt只剩3回合并会触发现有final-only收尾门槛。必需回岗但当前找不到路线时记录`return_route_unavailable`。若其他角色已覆盖全部武器、开拓者不需回岗，则不因这项窗口规则拒绝任务。白天炮手在能于入夜前到岗的候选中先选后侧/侧后操控位，再比较路线成本；若没有按时候选则仍保留最短路线回退。这是基于敌我基地方向的几何先验，不是墙体掩护的实战证明；夜间及已在武器相邻格的角色不为此重排。

S2任务效率首批在每次任务prompt中给出“剩余可用工具周期上限”：已知有效截止按`max(0,(remaining-2)//2)`计算，截止未知时只显示既有4次兜底额度的剩余额度，final-only显示0。该数字只是结合命令下发、结果回注和提交预留推导的上限，不鼓励用满，也没有改变原执行门槛、截止、回岗协调或已知长任务策略。每题另保留最后2组真实失败/空值/超时/截断的命令与结果，每组最多2048字符且命令、结果分别截断；它们独立标为失败或不完整观察，不进入成功证据、路径线索或下一题/下一局。提示要求明确路径直接读取，仅文件名时有界发现并尽量同命令读取；失败后须依据新证据改变范围或方法；API须逐字段对照当前文档和真实错误，进程`exitCode:0`不等于业务成功，也不能发明凭据。该有界记忆只降低失败证据被近期历史淘汰的风险，不保证模型不重试或提高成功率，收益必须由真实提交与得分验证。合法JSON中超长整数导致的`ValueError`按无效信封处理，后续合法响应仍可继续。

S2任务成功率首批只对“阅读/打开/read/open一个明确文件”的题面尝试一次确定性只读入口：完整路径只读原路径；纯文件名在当前沙箱工作目录内最多扫描6层、4096个条目和1秒，只有唯一普通文件才读取，正文最多32768字符，整条`executeCmd`不超过4096字符。命令使用平台基础`python3`标准库，不跟随目录symlink，不读symlink、FIFO或设备，不执行文件内容，也不访问网络。零匹配、同名歧义、权限/UTF-8错误、深度/条目/时间未扫完及正文截断均非零退出；截断另标`[TRUNCATED]`，不能进入完整成功证据。复杂题面、多文件、控制字符、窗口少于3轮、已有工具流程或final-only均退回原求解器；每题至多自动尝试一次。

同批在final-only阶段收到`kind=command`时仍禁止执行命令；只有已知至少剩2轮且本题未纠正过，才额外请求一次“仅基于已有证据回答、可靠部分回答或abandon”。第二次违规、剩余不超过1轮或无已知安全往返预算仍按原保护离点，不恢复探索、不延长任务或协调截止。外层`kind/content/complete`是工具协议，`answer.content`只放题目要求的结果；不全局强制内层JSON。工程完成须有真实check/TOKEN证据；API文档与真实反馈冲突时依据实际响应修订下一请求，不硬编码认证头、地点字段或真实题目答案。该改动减少入口往返，不保证模型遵循提示或实战成功率提升。

S2前三批在隔离分支上补充跨日恢复、新闻证据和候选解读。新闻解读每天最多使用2次普通LLM调用，给平台每日3次普通额度保留1次；只有当前对局仍有尚未尝试的新证据才发起请求，前日因额度未读的积压可在新日继续分析，已经尝试的旧消息不会因跨日自动重问。每次总计最多8条且受12000字符prompt上限约束，新证据优先；有剩余空间时可附带已分析旧证据作为`context`，只有`new_evidence`计入尝试。单条发送文本最多1024字符，`truncated=true`表示条件可能缺失。任务prompt、executeCmd和acceptTask优先；新闻返回只按本地pending、严格下一回合及requestId归属，不进入任务解析器。非法、空、迟到、跨局或伪造引用的返回拒绝且不自动重试；引用ID必须为字符串，引用片段必须来自本次实际发送的文本。候选最多保留32条，均为`pending_validation`，本版不会据此购买、献祭、改路、囤矿或改变防守动作。

S2防线恢复首批只复用原先最多6个固定安全墙目标：墙必须先在真实请求中被观察为存在，后续跨日白天确实缺失才重新进入待建；同日缺失、仍存在的低血墙、未知build反馈、明确build失败及永久不安全目标都不会被当成“昨夜毁墙”复活。恢复目标排在未完成固定目标之前，但仍受单目标每日一次机会、2墙批次、真实stone、当前安全/通路、资金与持券保护、Medicine、任务角色和回岗期限约束；不会每天追加新墙目标。低血现存墙仍交既有WallFixer链。

本批将固定蓝图扩展为迎敌正面加两侧连续14墙、背敌侧3个武器位、3个不同后侧炮手位和明确后方通道；方向仍以双方基地横坐标为首日先验，施工开始后保持稳定，夜间只记录我方来敌方向与先验的`aligned/opposite/mixed`偏差，不因单个绕后自动翻转或拆塔。墙仍按最多2格小批次逐段施工，并继续受真实stone、通路、资金/持券、任务角色和回岗期限约束。候选不足、静态障碍或旧塔偏离蓝图时必须标记降级并保留旧塔，不能把不完整布局称为完成。墙只阻挡移动，程序不假设墙能挡弹或保证机器人优先打墙。

本批投资目标选择不再把同物品的单位列表首项当成唯一目标。Weapon/Station/Wall升级券与WallFixer按当前我方合法目标枚举，比较现有出售、购买、使用和回岗完整路线；首目标不可达或超期时可选择后续可执行目标。已经启动的`fund`/持券计划优先保留其`useTargetId`，不会因单位列表顺序或另一目标稍近而换目标；目标毁坏、等级已变化、墙已修满或完整路线确证不可执行/超期后，按当前真实状态重选；搜索截断仍属未知，不等同于不可达。普通足额购券、单人筹资、联合筹资和持券使用使用同一目标语义，未改变物品优先级、价格预算、夜间追逐限制或1600次搜索上限。`decision.economyPlanning.investmentTarget`仅有界记录`item/targetId/selection(selected|continued)`，用于关联当次选择，不证明裁判已执行升级或修复。

缺位武器默认建rocket，已有武器加本轮预留总数仍不超过3，同轮两名工人可在不同蓝图格各建一座rocket；动态角色暂占固定塔位时等待或由既有协调移动，不回退到迎敌侧临时乱建。未知建造反馈不重复覆盖同格。已有1/2座rocket按剩余总槽继续，旧混合塔保留且只补合法空缺，不自动拆建。夜间每枚火箭先保证攻击我方基地、炮手或后方突破的紧迫来敌，再在同紧急层按剩余HP截断后的中心加邻格有效伤害选机器人实占格；同塔多枚与同轮多塔逐枚更新预计伤害。武器一旦开火，`targetPos`数量必须严格等于等级；有效目标少于导弹数时只允许重复已验证合法落点来填满本塔数组，后续塔仍按完整已计划伤害避免额外过杀。候选固定上限128，扫描中检查请求deadline；耗尽时只能返回完整合法数组或本塔不发动作，不能留下半数组/半更新预计伤害。该本地选择不证明真实伤害、溅射或存活率改善，必须由内网动作反馈与战损观察验证。

同批新增`decision.defensePressure`有界诊断：只统计`targetTeam`为我方的全图机器人，同夜按ID去重、跨夜允许ID复用；夜首轮观察才可标`complete`，晚开始、跳轮或超过每夜512个ID上限均标`lower_bound`。最多保留10夜和10条预测；召唤与自然增长无法拆分，统一标`combined_observed`。只有最近两个相邻且完整的夜样本才按类型使用“最新量+最新非负增量”作为下一夜参考；某类型下降时将其列入`uncertainTypes`，准备参考不低于这两夜该类型的较大观察量，但不累计更早历史最大值，也不宣称这是上下界。首夜、缺夜或partial同样不得推导更安全。战损只有“夜首轮完整+黎明首轮”同时具备才标`complete`，否则保留观测差并标`lower_bound`。白天显示当夜预测，夜间显示下一夜预测；旧预测在目标夜到来后保留实际量与绝对偏差，不用结果回填预测。`known_deficit`同时考虑当前固定墙、受损墙、炮手覆盖及上一夜可观察战损；`unknown`和`no_observed_deficit`都不等于保证存活。该诊断本批不自动购买、加墙、抢资金或延迟回岗。

已有内网证据口径：600517的R21工具检查6/6及结果token、R22真实非空`submittedAnswers`、R23对应动作反馈成功/任务冷却/金币增加80，与`goldReward=80`满额一致，可作为至少一类工程任务完整成功正例；该结论来自跨回合链，不是只靠模型`complete`自报。600588的R131基地HP实为1375，85是R243末值，撤回“首夜濒危”口径；R147/R159的`commandRequested=false`且`executeCmd`length=0支持硬拦截正常。defender阵营仍未覆盖，不因工程任务正例将S1或S2标为已通过；600517末余1345与旧报75冲突，也不据此声称第二夜稳定。

内网Agent负责获取版本、上传、发起对战、分析和GitHub issue反馈；代码由外网开发Agent修复。原始日志不能回传，保留在内网供复查；issue只写允许传出的最小事实摘要。无法披露的字段明确标“受限不可提供”，不要改造成虚构数据。

## 2. 获取版本与启动

1. 阅读仓库AGENTS.md。运行`git status --short`、`git rev-parse HEAD`、`git remote -v`，确认仓库为icesword007/NightWatch。工作区有修改先报告，不覆盖、不reset。
2. 在干净工作区按用户安排拉取目标版本，例如`git pull --ff-only origin main`，再保存完整HEAD SHA。拉取失败不强制覆盖。
3. 记录`python3 --version`。赛方环境说明为Python 3.11.10，本地开发已验证3.13.3；内网须验证实际3.11环境。程序使用标准库，无需pip安装。
4. 候选上传根目录为CoreGeek内容，其中run.sh、main3.py、src/agent/*.py必须保留相对布局；确认平台解包后实际运行位置。若平台要求不同，先报事实和所需调整，不擅自改程序。
5. 启动方式：在CoreGeek目录执行`bash run.sh <平台指定端口>`；监听0.0.0.0，HTTP POST根路径。不要把尖括号占位文本当参数执行。
6. 可以先在CoreGeek执行`PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest discover -s tests -p 'test_*.py' -v`，预期393项全数通过（含16项HTTP回环）：367项投资协同基线、7项墙升级与19项任务入口/收尾增量。本地通过不替代对战。
7. 上传仅包含实际运行文件，排除.git、.DS_Store、__pycache__、*.pyc及本地环境文件；无需删除工作区文件。记录打包所依据SHA、包SHA256（工具可用时）、包含文件清单和启动设置。平台如另有构建步骤一并记录。

本次没有内置对局ID字段，也不能仅靠buildId判断代码版本。若上传包有任何未提交源码差异，必须注明“修改版”，列明差异，不能称原SHA已通过。不要在记录中暴露凭据或带token的URL。

本地性能参考场景为41×32地图、R30、两名工人分别位于(2,1)/(2,3)、各持10份copper、售价5、基地券和武器券均为100。r6曾将`next_step`调用由9474降至970；本批为比较同物品多个完整目标路线，当前同机无profiler实测1410次（请求级`pathSearches=1408`、未截断、约0.219秒），仍低于1600次经济上限，响应动作保持为两名工人分别移动到(3,1)/(3,3)。增加的搜索是多目标完整链比较成本，不上调预算；该数据不是裁判端到端时延，也不能替代V04。

## 3. 必测点与结果状态

每点使用“通过 / 失败 / 未覆盖 / 证据不足”，不能把没报错等同通过。

| ID | 观察与通过条件 | 不足时怎么处理 |
|---|---|---|
| V01 | 目标版本在官方环境成功启动，端口和绑定正确 | 启动/导入/打包错误直接报错，不换对手重复撞同一错误 |
| V02 | 连续至少2个真实回合请求获得完整JSON响应；记录观测窗口内裁判侧异常数 | 请求中的`errors=[]`只表示该次请求未携带错误，不等于裁判累计零异常；没有裁判统计就标证据不足 |
| V03 | 至少一次move真实生效：指令角色属于当前存活工人/开拓者，目标相邻且静态可通行，下回合位置与反馈支持移动成功 | 碰撞单次失败不算协议异常；查目标争抢或障碍，改条件补测 |
| V04 | 记录裁判侧响应耗时，确认观测窗口未触发5秒响应超时 | 只有程序日志时标裁判时延不可得；服务端指标不等于端到端时延 |
| V05 | 两个阵营分别核对角色ID、地图方向、移动效果及V10资金链；必须逐阵营记录实际动作链，获胜或存活本身不算资金链通过 | 未跑到某阵营标未覆盖，安排换边/其他可用对局；不能用本地阵营等价测试替代内网实局 |
| V06 | 有移动指令时，空字符串prompt和executeCmd被裁判接受，无错误调用或额度异常证据 | 若无额度观测，只能确认响应形式被接受，不能证明额度没扣 |
| V07 | 自然出现无可移动角色时，roleCommandMap为空的响应被裁判接受 | 未出现则未覆盖；不改代码制造该场景，不无限重试 |
| V08 | 能关联构建、SHA、对局、阵营、回合、输出动作、受控角色位置、双方基地状态与下回合反馈 | 缺证据时补日志分析能力或平台资料，不猜失败归因 |
| V09 | 至少一名工人真实完成“采集→背包出现→出售→金币增加”；有多个当前可见矿点时，选择应结合正式售价与从当前格到采集、兑现的实际动作成本，明显更高的可兑现价值/回合可替代最近矿；路线或报价变化后重评，采集失败或矿点消失后不无限重试 | 分步记录目标矿种/坐标、`decision.actions[].economy.estimatedValue/estimatedRounds`、工人位置与背包、正式售价、动作和下一回合反馈。每次collect只按1份滚动估计；赛方确认每矿最多10次后消失、下轮随机刷新，但请求不提供剩余次数，因此不得假定当前矿尚有10份，也不得把未采资源计作资金 |
| V10 | 使用当前实价完成单人或两工人“出售确认到账→唯一买方购买→背包出现→使用→两人回到不同炮塔”；同物品有多个合法目标时，首个目标不可执行不应遮蔽后续可及时兑现目标，已启动目标在仍合法可执行时不得因列表顺序或小幅距离变化切换。已有普通采矿计划的工人在当前库存足额、完整路线及时后须切入单人资金链，施工者仍继续施工。联合筹资只在正好两工人、三塔已齐、双方当前真实库存合计可承担且完整双路线赶得上截止时启动，个人可独立承担时仍走单人链路。买方已经持券后，即使协作者死亡、缺席或联合计划失效，也应按当前合法性独立使用并回岗；夜间只允许原地合法使用，不追逐远处使用点 | 逐轮记录采购目标/使用目标、参与角色、各自当前库存估值、旧计划、`expectedContribution/currentGold`、`economyPlanning.investmentTarget`中的物品/目标ID/续行或新选原因，以及寻路/截断/联合状态/持券状态/仲裁拒绝数、两条实际路线、绑定炮塔、动作和反馈。目标毁坏、等级改变或墙已修满后允许重选；deadline或搜索截断未知不得自动归因为目标不可达。`jointStatus=return_deadline`表示找到路线但完整返岗链超期；`sale_route_unavailable`、`purchase_route_unavailable`、`distinct_post_unavailable`和`action_conflict`分别指向当次规划的固定有界阻断，不得延伸成对局根因。第二笔出售失败或未反馈、角色死亡/被自救占用、涨价、需求消失、路线或截止失效时不得用预计金币购买；核对`jointCancelReason`，不得重复消费共享金币或让两人占同一炮位。若持券者没有动作，先按诊断区分资金不足、持券待兑现、寻路截断和动作被仲裁，再补最小上下文；不能仅凭复测样本断言完整根因 |
| V11 | 白天建塔仅落在基地蓝环可建格，优先背敌侧3个固定蓝图位，默认均为rocket；已有武器加本轮预留总数不超过3，同回合两名工人目标不同且金币不超支。已有1/2座rocket续建剩余槽；旧混合塔保留、偏离蓝图时标降级；未知或失败反馈不无限重发同一格 | 记录矿价、出售与金币到账、三座规划/已建/缺口坐标、实际路线、build反馈和`layout.degradedReason`。逐阵营核对后排镜像，不用配图坐标替代真实蓝环合法性；裁判规则不一致建“规则待确认”issue |
| V12 | 通常三塔完成后最多一名工人进入稳定施工链；两塔阶段只有至少两名工人且其中一名已真实持stone时才允许该施工者先做连续正面段，另一工人继续第三塔筹资/建设。无随身stone或只有一名工人时第三塔优先，不为墙先开采石链。每批最多2块并继续到14格正面加两侧蓝图；资源不足、矿消失或临夜时按完整施工与返岗路线缩短/停止，不再使用固定12轮一刀切。曾真实出现且跨日缺失的固定墙优先恢复；蓝图预留3塔、3炮位和后方通道，施工中与完成后不得困住工人或阻断回岗；fund/持券/Medicine/任务角色不被抢占 | 逐轮结合`decision.economyPlanning.fortification`及其`layout`核对方向依据/观测偏差、规划/已建/缺口、塔位/炮位/出口、`batchTargets`、builderId、phase、skipReason与实际动作反馈。双边完整跟踪“2塔+持石墙段→另一工人筹资→第三塔→14墙→三人入夜到岗”，并记录每次临夜继续工作的完整剩余动作、唯一绑定炮位、实际返岗步数、2轮余量和最终余料。动态占位不得改变固定塔位；永久静态不足才降级。围墙是否免伤/挡弹仍未知，本地可达不等于实战有效 |
| V13 | 黄昏前3名炮手分别到3座塔的一格内，优先蓝图后侧不同站位，但实际威胁、可达和到岗期限高于坐标整齐；重叠可控范围采用角色、武器、精确站位均唯一的一对一匹配，同日保持有效绑定，列表重排不换岗，绑定失效才重算。夜间已在合法安全位不为蓝图重排。冷却中守岗不等于释放炮手；开拓者第三岗与任务回程规则保持 | 按回合记录每座武器/控制者ID、Chebyshev距离、蓝图炮位/实际炮位、通道、当前目标/冷却和到岗期限。核对施工、经济与防守同帧使用相同绑定；有效资金链预留的角色/炮位不被普通分配占用。双边分别记录炮手首个受伤/死亡回合。单邻格局部扩展上限可继续其他候选，全局deadline后立即停止；无合法目标可空命令守岗，不以一次attack证明长期到岗 |
| V14 | 夜间rocket存活、冷却为0且controller相邻；一旦开火，目标数组长度严格等于等级，每项均为射程内真实机器人格。优先我方基地/炮手/后方突破威胁，再比较中心20、邻格10按剩余HP截断的预计有效伤害；同塔完整数组更新后，同轮后续塔不再把已预计死亡目标当有效目标。有合法有效目标不故意空炮，HP≤40且持Medicine仍先自救 | 逐枚记录目标坐标、目标阵营/HP、相邻机器人、预计有效伤害、反馈与下一回合HP变化；目标少于等级时核对合法重复填充，不能出现半数组导致整次被协议拒绝。大量机器人与41×32场景同时观察响应预算、候选截断和deadline退化，不因收益选择越射程或转打对手机器人而放弃本方紧迫威胁 |
| V15 | 我方基地缺席/零血样本不再生成基地建造、升级或召回；残存角色/武器仍只发合法动作 | 样本未出现标未覆盖；不为取样修改对局请求 |
| V16 | 开拓者只向本方`isValid=true`且冷却为0的任务点移动，并在相邻一格后发出`acceptTask`；需开拓者回第三炮位时，领取前须有“到达+至少一个完整命令/结果周期与提交+实际回岗路线”窗口；任务2若显示两格，记录实际可领取格 | 记录任务类型、任务点、前后位置、领取动作、`decision.taskStartSkipReason`与下一回合反馈；不能用出现题面倒推裁判反馈为true |
| V17 | 领取后出现非空`phaseTask`；若题面保守识别为单一明确文件且预算允许，同轮直接发出一次有界只读`executeCmd`，否则走原非空`prompt`。已知截止时prompt携带当前剩余轮数、是否允许命令探索及剩余工具周期上限。完整路径只读原路径；纯文件名在当前目录最多6层/4096条目/1秒内查找并读取唯一普通文件，正文最多32768字符，命令最多4096字符；多文件、歧义、未扫完、非普通文件或截断失败关闭。API任务逐字段对照当前文档与真实反馈，冲突时按实际反馈修订，不因shell退出0声称业务成功，不发明认证。工具进程完成后的输出作为有界观察证据并可提取最多8条环境路径线索，新题须复核、换对局清空；失败结果和LLM命令中的猜测路径不得进入线索。返回只接受command/answer/abandon严格JSON信封 | 在内网先用team/round/buildId和日志顺序关联`task_detail`与`turn`；核对首次响应究竟是自动读取还是prompt，`decision.taskEntryReadAttempted`只说明本题已作入口判定。实际执行失败、歧义、非普通文件、扫描或正文截断必须非零，且不得进入Critical tool evidence。正文仅用于内网定位，不得贴issue。分别记录原题真正完成证据与最终答案信封格式，不用`complete:true`自报替代裁判反馈 |
| V18 | `kind=command`只在预算允许时作为同回合顶层`executeCmd`交给平台任务沙盒；下一回合`lastCmdResult`再形成新prompt，本机无命令执行。截止未知的任务最多接受4个命令周期；已知截止且剩余预算足够时可继续第5个及后续非重复命令。prompt显示的是可用周期上限而非目标：已知剩余4/5/6回合分别为1/1/2，回岗协调截止取更早值，final-only为0。真实任务截止剩3回合的prompt不得再开启命令，但剩4回合prompt已请求、剩3回合到达的在途命令仍执行，结果回注后要求完整/部分答案或abandon | 用`task_detail.flow`区分本轮输入和本轮输出，逐轮记录显示上限、实际请求数、无效重复、最终提交和真实得分；同回合或迟到结果不得串入。平台已标`[TRUNCATED]`时不能称完整原始输出；核对14游戏回合链在剩3回合的已知截止触发收尾，`complete=false`失败反馈在截止内仍可改进，不把工具输出自动拼成答案 |
| V19 | `[exitCode:0]`只表示沙箱工具进程完成，可作为输出观察继续求解；API正文中的401等业务结果仍须模型结合原文判断。空值、非0、`[TIMEOUT]`、`[JUDGER_ERROR]`、`[TRUNCATED]`均不被当答案或任务成功。最近2组失败/不完整命令与结果应在同题后续prompt持续可见，长结果在原预算内保留退出状态、开头上下文和末尾错误，中间明确标省略；新题或换局清空且不得进入路径线索 | 当前题已关联的同一命令连续两次明确非零退出后，第三次原样命令不得下发，只在截止与final-only允许时请求至多一次按末尾错误改法、给可靠完整/部分答案或abandon；再次原样命令即停止。结果正文变化不绕过，命令逐字符不同并实际下发后重置旧链；exit0、未知、迟到结果或正文错误关键词不计明确非零。用`decision.taskConsecutiveNonzeroFailures`与`taskRepeatedCommandCorrectionRequested`辅助关联，但真实收益仍按提交和得分记录 |
| V20 | `kind=answer`形成开拓者`submitAnswer`；`complete=false`可提交可靠部分答案；同一开拓者同回合不会同时提交与移动/攻击 | 记录答案是否完整标记、动作反馈和后续任务状态，不回传答案正文；提交动作成功不等于题目完整通过 |
| V21 | 活动任务开拓者不被普通维护移走，可原地用Medicine；已贴有效塔时可立即开火。远程紧急回援仅在夜间、明确我方威胁距基地不超过3、存在冷却0且能打中该威胁的无人塔、开拓者可赶到而等待一回合即错失、且没有空闲替代者时移动优先 | 记录任务/基地/塔/开拓者/威胁位置、目标阵营、路线回合及连续动作；值守其他塔的炮手不算空闲替代者。距离3为待校准阈值；任一条件不满足不得召回 |
| V22 | 任务结束区分超时、开拓者死亡、离点和未知；至少一类任务取得裁判可核实的完整成功证据，并核对金币/积分时序 | `phaseTask`消失或奖励变化单独都不足以证明完整成功；缺正向结果字段时标证据不足并保留issue |
| V23 | 无活动任务且当前对局存在尚未尝试的新新闻时，响应仍保留原角色动作并发出带requestId的有界prompt；下一回合`event=news_detail`记录同一requestId、`newSourceIds`、`contextSourceIds`及接受/拒绝原因。单日最多2次，第三次不发；前日未尝试积压可在新日恢复，已尝试旧消息不得重问。候选必须保留引用且状态为待验证 | 只核对调用、归属、配额和日志，不按候选执行宝藏或经济动作。模型不返回严格JSON、引用不匹配、错误码5、长转义文本缩批或真实消息更新模式不明时记录实际发送来源、原请求日与拒绝原因，不能宣称新闻已证实或宝藏已解出 |
| V24 | 连续至少两夜核对`decision.defensePressure`：目标我方的分类型数量、完整性、首末观察轮、战损、当夜/下一夜预测、依据日与后验偏差；并将跨日缺墙与V12实际恢复动作关联 | 夜首轮未收到、ID截断或中间夜缺失时必须标`lower_bound/unknown`，不得按确定公式外推。召唤叠加只标`combined_observed`，不能拆成自然增长。`rising_pressure`是保守参考，`no_observed_deficit`不是安全保证；若没有跨日真实毁墙，恢复项标未覆盖，不人工破坏请求 |

S0/B回归需启动、连续请求、确认动作效果和可核实的响应/异常证据；C批至少完成V09–V14中实际出现的关键链路；D批必须走通V16–V20，并为V21–V22取得可触发部分的证据。首夜“发出合法攻击”与“基地存活到天亮”是两个独立结果，前者不能替代后者。V04没有裁判侧耗时就必须写证据不足；V12只允许把全队一次墙试验记为一次试验；V13只有在最晚到岗回合前实际抵达并形成有效操控才算按时。条件性场景未触发时单独保留缺口，不将其他观察作废，也不能声称所有边界已通过。

## 4. 如何读程序日志与分析

每个请求固定输出一行`event=turn`摘要；任务相关请求另输出一行`event=task_detail`详细取证；新闻请求或返回另输出一行`event=news_detail`。摘要字段保持buildId、UTC时间、roundNo、team、经济（含`score`）、角色/建筑、机器人、商店、基地、commands、actionFeedback、task、decision和timingMs。新闻prompt、原返回及候选只进入有界`news_detail`，不会混入`task_detail`或`turn`摘要。`task_detail`和`news_detail`含`requestFingerprint`；`event=turn`不含该字段，应用team/round/buildId和日志顺序关联，不要求在turn中寻找不存在的指纹。任务详细事件只含白名单任务交互字段，并以taskInstanceId关联。`inputAssociation`按字段标注：本轮`phaseTask`与本轮响应中的prompt/executeCmd/提交可标当前活动任务；请求里的`llmResp`和`lastCmdResult`只知来自先前请求，在没有可证明实例ID时标`unknown_previous_request`。不得把它们强行归入当前新任务，也不允许根据相邻行猜成确定事实。

`decision`和`event=turn`仍不含题面、prompt、答案、命令、LLM响应、沙盒输出或新闻正文。正文只存在独立`task_detail.text`和`submittedAnswers`。每个正文字段含value、inputType、originalLength、truncated、platformTruncated和完整输入指纹；日志自行截断与平台上游截断必须分开。实际接受动作的细节位于`decision.actions[].economy`；请求级固定诊断位于`decision.economyPlanning`，除既有路径/资金字段外新增固定上限的`fortification`。这些字段可解释程序选择边界，但不能单独证明裁判执行结果。

`decision.economyPlanning.fortification.gunnerStands`才是炮位几何列表，不在`decision`顶层。新任务因求解/回岗窗口不足而未领取时，`decision.taskStartSkipReason`记录有界原因。这些字段只解释当次选择边界，不能单独证明裁判执行结果。

`jointStatus`是多个候选阻断按固定优先级选出的摘要；`return_deadline`只证明至少一个候选的完整路线超期，不表示所有候选都只因期限失败。`truncatedReason`汇总整次经济提案的寻路截断，后续路径出现的截断不能自动归因到联合筹资。

所有日志列表使用`items`加`truncated`，每类最多16项；动作目标最多3个，并单列`targetPosTruncated`。背包只计stone/iron/copper、Medicine、WallFixer、Bomb、DizzyWeapon及Station/Weapon/Wall两级升级券，未知物品不写日志。命令结果只分`none/completed/failed/timeout/judger_error/truncated/unknown`；`completed`仅指退出码0，不是业务成功。基地不存在时只记录`present=false`，不能解释成零血。commands属于当前回合；actionFeedback来自上回合动作，必须跨回合关联，不能当当前动作结果。

processing为服务端读取解析到编码的时间；serverWriteComplete为服务端写入响应完成的时间，二者均不代表裁判收到响应的耗时。日志在写回响应后输出。buildId当前是固定标签，真实SHA从上传记录提供。

例：回合r的`controlledRoles.items`显示工人W位于(x,y)，`commands.items`输出move到(x+1,y)；回合r+1反馈W=true且角色位置变为(x+1,y)，可认定该次移动生效。若W=false，结合目标位置/其他单位行动辨别碰撞或阻挡；证据不够就列候选原因与缺失观察，不断言程序错误。双方基地的实际路径是`bases.our`与`bases.enemy`，可证明请求中基地是否出现及其当时血量，但不能单独证明最终胜负或终止原因。白天R66–70没有攻击不能记为射击延迟；还须在夜间同时核对射程、目标和冷却。动作反馈false必须先关联到前一回合owner和action；已知两条false是move时，不得改写为attack协议失败，具体失败原因仍需位置与占格证据。

程序摘要可以直接证明：该回合请求里观察到的金币、分数、白名单库存、己方建筑、有限机器人、商店价和任务点；程序输出的动作类型/目标与工具调用形态；下回合请求携带的按ID动作反馈、错误码与工具结果类别。任务点的`scoreReward/goldReward`只是奖励元数据，不是实际到账；奖励必须结合提交前后`economy.gold`与`economy.score`（请求原字段为`teamOur.goldNum/totalScore`）及同轮其他收支判断。连续回合结合位置、库存、金币、HP和反馈，可形成V09–V21的动作链证据，但任何`truncated=true`都表示该类观察不完整。

必须从裁判页面或内网额外证据取得：裁判端到端耗时与累计异常数、对局最终胜负/终止原因、任务答案真实通过率与“完整成功”、奖励变化是否由某次提交导致、平台是否持久化沙盒、围墙受击与炮手伤害顺序，以及任务结束结算规则。详细正文日志只解决语义定位证据不足，不自动证明提交正确或任务成功。

B批公共基础在本地已有单元与HTTP测试；其中动作/工具结果只关联严格前一回合，同回合替换响应会撤销原待确认记录，锁竞争超出内部预算时返回当前请求独立生成的基本响应且不修改共享状态。平台侧只验证它没有破坏真实协议链路。若平台可安全重放同一请求，可记录同回合同正文响应是否完全一致；若平台不提供这种能力，标未覆盖，不修改裁判请求、不用代理伪造流量。换边后重点确认首次响应正常且日志team与实际阵营一致；内部动态状态是否清空仍以本地测试为主，平台没有状态观测接口时不要虚构证明。内部截止是主动软截止，不能中断已经开始的Python解析或系统调用；仍以裁判侧观测判断5秒限制。

区分四类：启动/程序异常；格式或未知指令导致的参赛异常；字段完整但因碰撞等未生效的游戏动作；规则或证据未知。赛题规定异常累计5次停调，程序异常退出不再拉起；合法指令执行失败不等于队伍异常。程序返回HTTP400/500不是合法回合兜底，应记录触发请求的允许披露字段与错误摘要。

## 5. 本轮送测边界

用户已授权统一r2推送和内网验证，按【codex】s2-integrated-r2-intranet-prompt.md及外网提供的完整SHA执行一场定向对战。已有Bomb/Dizzy无库存或宝藏无自然结构化候选记未覆盖，不伪造输入或更改源码。独立430项回归（含16HTTP）通过10.412秒，原入口/波次反例及黎明定稿复核通过；这不替代平台验证。

旧e084aed及更早版本不再跑、不再补查日志，历史结论保留为基线；旧手册中指定旧PK、旧回放、旧6墙或混合阵容的任务均不再执行。新版没有自然触发墙券门控、final-only纠正或第三夜时明确标未覆盖，不改请求制造条件。

不得因胜负/总分单项改善宣称S2通过；双阵营1300回合基地存活的总体目标继续保留。当前小批验证先确定增量链路与真实瓶颈，不要求无限刷十天。

## 6. GitHub issue反馈模板

先查已有issue，复测在原issue补评论；独立问题单独建issue，首次总体结果可在相关原issue给出索引。标题建议：`[S2][现象] 简短描述`。不要只写“有问题”或“已解决”。

```text
类型：验收记录 / 缺陷 / 规则待确认 / 证据缺口
参赛完整SHA：
构建标识：nightwatch-s2-integrated-r2
工作区/上传包差异：无，或逐项说明
包SHA256与启动方式：
Python版本：
对局ID、阵营、对手及可得版本：
地图/种子：可得值，或不可得
观察窗口：回合…至…

逐项结果：V01…V24，每项通过/失败/未覆盖/证据不足
期望：
实际：
最小证据：UTC时间、回合、阵营、角色ID、前位置、输出指令、后位置、双方基地状态、反馈/错误码、任务阶段及工具结果类别；题面/命令/输出/答案正文只留内网，issue不含原文
服务端耗时 / 裁判耗时：分别记录；不可得不要填0
程序异常与动作执行失败：分别统计，注明数据来源
分析：已证实事实 / 推断 / 无法排除的解释
内网证据索引：内部保存位置或代号，仅传允许信息
复现条件及次数：
已补测对手与结果：
仍缺哪些观察，建议如何补测：
验收条件：修复后需看到的明确结果
原issue / 被测修复SHA：
```

首次同时记录上传开始、对战开始/结束、日志可分析、issue提交时间（含时区）、人工耗时，以及可取得的日志粒度、固定种子/批跑能力、排行榜与对手信息。用于估算仿真收益，不虚构缺失时间。

## 7. 修复复测闭环

外网主Agent读取issue→核实问题→开发修复并推送→内网Agent按约定触发复测→原issue补完整被测SHA和验收结果。不是自动定时任务，本手册本身不创建监控。

修复后选能复现问题的对手/条件，并保留基本启动与移动回归。全部对应验收条件有证据通过才关闭原issue；任一未通过继续open。拉取或推送成功、版本号变化、一次对战获胜都不是关闭依据。无法确定如何取证时反馈缺口，外网主Agent决定下一步。


## Issue归档迁移约定（2026-09-15，用户授权）

- 用户批准因消息体过长将旧open issue #4/#5/#6/#8关闭归档；这是迁移，不表示缺陷修复或S1验收通过。关闭前保存全部正文与评论快照，远端保留旧内容，并在关闭评论中说明原因；关闭原因使用not_planned以避免标为已解决。
- 后续由内网Agent对真实剩余问题建立独立issue：经济兑现、任务完整求解、炮手防线与defender首夜分别跟踪，按实际根因再拆分。每单一项明确验收目标，链接原issue，不复制旧全文；r8实证与r9本地修复状态分开。
- 团队建议正文约2000中文字符以内、单条增量评论约1500字符以内（不是平台硬限制）。正文只保留当前结论/验收，评论追加本次增量，不同时重复贴正文与评论。历史原文留旧issue，完整日志留内网；超长证据按独立子问题拆分。
- 新issue在实际复测通过后关闭；不得将此次迁移例外扩大成“未通过也可按完成关闭”。新编号由内网创建后回传映射。


### 本批本地证据边界（R138）

布局批343项本地回归通过；本次投资目标批累计360项独立回归通过。完整14墙/第三塔/入夜回岗连续测试从已有两塔、工人持14石及另一工人持待售铜开始，证明该状态后的施工与回岗链可执行；不是裁判仿真，不证明真实初始资源下首日必然建齐。内网应另核对实际采石数量、筹资/行走/施工耗时和首夜已建比例。两塔未齐第三塔时，只有已有持石工人且另一工人可继续经济，才启用交错筑墙；无随身石头仍优先第三塔，避免提前采石挤占筹资。

本次投资目标批本地覆盖首目标遮蔽、有效目标绑定、封死/超期/毁坏/修满后重选、截断保留候选和第二目标购券至使用连续链。多目标比较增加搜索工作，仍使用原1600次上限及请求deadline；内网须观察实际截断与时延，不能由本地回归推导升级收益或生存提升。


### 投资协同增量（nightwatch-s2-investment-r2）

本批在r1目标选择基础上协调多名工人的建筑投资。每轮从当前有效计划推导目标的执行者，优先保留有效在途分工；已有重复计划按真实持物品与稳定角色ID消解，新计划也采用确定顺序。其他独立工人选择不同目标，同建筑日夜不重复使用升级券或WallFixer；联合筹资由唯一买方代表，两名参与者不互相排斥。Medicine仍允许各自自救。

验证时结合decision.actions的roleId、reason及economy诊断、背包/金币和实际反馈，跟踪“两人不同目标买券→持券→使用”。只有一个合法需求时不重复启动采购；不同目标仍可并行。持有者死亡/缺席、计划过期/旧session、use物品消失或目标不再需要时，不能持续占用目标。临时预选可能保守影响本轮另一角色，但不会作为独立持久锁写入下一轮；只有既有最终接受的计划及真实状态影响后续分配。真实响应耗时与收益待实战，不宣称全局最优或跨人背包转交。


### 任务成功率增量（nightwatch-s2-integrated-r1）

本批只缩短明确单文件题面的入口读取，并为final-only违规提供一次有预算的收尾纠正。内网逐题记录题面是否满足保守识别、首次响应是自动`executeCmd`还是prompt、读取状态、后续真实check/TOKEN或API响应、最终答案信封和裁判反馈。明确区分“成功读到题面”“工程/API步骤真实完成”“答案格式正确”和“裁判确认成功”；任一前置成功都不能替代后者。

自动读取失败、同名歧义、非普通文件、未完成扫描和正文截断必须非零退出，不能进入成功证据。final-only纠正只允许一次且不执行违规命令；第二次违规或仅剩1轮应停止。未自然遇到的边界标未覆盖，不注入或修改真实对局输入。该版本未加入通用工程修复器、任务专用答案、Bearer/location硬编码或内层答案全局JSON校验。
