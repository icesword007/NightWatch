# S2统一增量版：本轮内网执行手册

日期：2026-09-16。仓库icesword007/NightWatch，分支main，构建`nightwatch-s2-integrated-r1`。本轮停止旧版补证，直接部署新版；旧提示仅保留历史，不再执行旧PK提取任务。

## 1. 获取并锁定参赛版本

先读AGENTS.md、本文和【codex】intranet-validation.md第0节及日志规范。检查工作区后`git pull --ff-only origin main`，不覆盖本地改动、不reset。确认`776e8accac04e62dc6c4f0cb28ec3fb403c2d685`和`5810d6d5a5c79d7f7dea89b0c4e4cd71b3a0a3db`均为HEAD祖先，并核对构建；记录实际完整HEAD、包SHA256、文件清单、Python版本、启动设置和任何本地差异，构建名不能代替SHA。若外网另提供送测SHA，必须与之核对；拉到其他新版先报告版本差异。

上传CoreGeek运行文件并保留run.sh、main3.py、src/agent相对结构，排除.git/.DS_Store/__pycache__/*.pyc/本地环境文件，不删除原工作区缓存。平台按已有流程启动；标准库无新依赖。本地合并独立393项回归通过（含16项HTTP，9.826秒），内网可按通用手册命令核验实际Python环境。不要中途替换运行中的上传包。

相对上次e084aed，本次新增投资目标完整路线和承诺保持、多工人不同目标协同、墙升级采购、明确单文件入口读取及一次final-only纠正。没有调整采石选择或火箭目标算法；不先假定旧局所有问题已修复。

## 2. 小批定向对战

先2场，优先合适对手换边，实际阵营从team字段确认。选择能触发任务/投资、尽可能运行到完整第三夜的对手；不按排名机械选最强。只为明确缺口最多加2场：另一阵营、任务类型、门控可触发或更完整防守窗口。无新增信息、稳定同处失败或启动/协议错误则停止并汇报，不刷到出现想要的结果。

每场记录PK、SHA/build、对手、阵营、结果、实际末轮与基地HP、终止原因。对手先死导致结束不等于我方完整第三夜或十天已通过。S2双阵营1300回合基地存活仍是总体目标，本次仅验收增量与定位瓶颈。

## 3. 按优先级分析完整日志

### A. 任务成功与金币收益（#10）

逐实例统计接取数、提交数、完整成功数、部分奖励数、无提交/失败数及实际金币/积分。任务ID跨局相同不代表同题，保存题面指纹和内容判定的类型，不依赖不存在的type字段。

对一个成功及每类失败代表，给出：接取→首次正文可用→每轮prompt/LLM返回/executeCmd/结果→收尾→submitAnswer→裁判反馈/到账。正文读取使用task_detail.text各字段.value，flow不是正文；关联turn.decision实际剩余预算/协调deadline。先检查字段实际类型；指纹仅用于detail去重，不能要求turn存在requestFingerprint。

- 明确单文件入口是否首轮直接读取，实际成功是否提前；taskEntryReadAttempted不是已执行或已读成功证明。显式路径、唯一匹配、扫描/正文截断失败分别记录；不支持的题面应回原LLM。
- 工程题必须完成真实检查并取得题目要求结果，不能提交步骤代替token；外层kind/content/complete合法与内层答案符合契约分开。API文档可故意过时，核对实际请求/反馈后的合理调整，不硬套Bearer或location。
- final-only如果返回command，是否在已知剩余至少2轮时仅一次纠正、命令未执行；剩1轮不纠正、不延后回防。若未自然发生，标未覆盖。错误answer本身不会被此机制自动修复。
- 若仍失败，内网GLM 5.2基于完整题面与交互定位第一处可证明的偏差，分别评价可用工具时间不足、模型行动错误、业务未完成、答案契约不符或裁判未知。提出最短可行路径和有证据的修改建议，不只给退出标签；缺证据明确指出。

### B. 投资兑现及墙升级（#9）

按真实价格与动作反馈闭合“初始金币+采矿出售+任务奖励−购买/其他支出=余额”。不要把任务奖励元数据当到账；每条链核对sell→确认金币→buy→持券→到合法目标→use→等级/HP变化→回岗。

观察两个工人是否保留不同目标、只有一个需求时不重复买用、有效在途目标不因小幅距离变化反复切换；目标死亡/满级/修满/owner死亡或计划失效后能否释放。搜索截断是未知，不直接解释为不可达；记录与真正失败的关联。

墙券新采购须三座武器与session固定墙目标全部真实建成，再在既有候选之后考虑券1/2。未建齐、不足资金、路线不及时分别记录，门控没触发记未覆盖。已持墙券不受新采购门控阻断；自然触发时核验L1→L2、L2→L3和满级不再买，同一墙不重复采购/使用。不假设存在未来武器资金硬储备；观察墙升级是否挤压更有价值投资，建议需附当轮可执行条件。

### C. 施工、防线和第三夜（#11）

用新局记录基地/三塔/固定墙目标/炮手坐标及真实墙完成数，首墙和每夜前时点。施工者从选矿前一轮至首墙逐轮列位置、原始背包类型与stone计数、可见矿点类型/坐标、失败排除（可得时）、实际collect/return/build和下一轮反馈。近矿不选先证实当轮存在且未被排除；摘要空背包与成功建墙矛盾时核对原始对象，原始不可得就标证据不足。

第一次基地失血前后数轮，列墙缺口、三塔等级/上一发/冷却/操控者、机器人目标与位置/HP、炮手HP、每枚火箭落点。用完整可得输入核对落点3×3所有机器人，不用16项截断摘要断言打空；完整输入不可得明确限制。roleId可能为武器ID，controllerId才对应炮手。分清紧迫威胁、有效群伤、预计过杀与下轮实际伤害，不先认定优先打大型一定最佳。

波次按targetDay与basisDays匹配，actualCounts=null可能是未来未观测；不得硬编码第三夜大型数量。区分墙未建齐、资源不足、冷却空窗、选点问题、方向偏差，给有证据的主要瓶颈及其他可能因素。

## 4. 回传、停止与交付

完整日志/题面/请求/答案/凭据留内网。先本地分析再向#9经济、#10任务、#11防线追加本次增量；单条约1500中文字符内，不重复复制正文。新独立根因或超长问题可另建issue并关联原单，不自动关闭任何issue。

每项标通过/失败/未覆盖/证据不足。每条结论给参赛完整SHA、PK/阵营/回合、最小脱敏事实、分析推断与缺口、建议及明确复测条件；区分新局与旧e084aed历史摘要。不要传token值或原始全文，不因complete自报、无errors或获胜宣称全部通过。

汇总本批覆盖矩阵与下一步最值得修的1–3项；未覆盖项只有预期新增证据明确才建议再跑。首次顺手记录上传→对战→日志可分析→issue反馈各时间及人工耗时（未知写未知），帮助评估内网周转与本地仿真价值。
