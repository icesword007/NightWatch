# 当前任务：统一r6双阵营验证

批次R218-integrated-r6-test-01；构建nightwatch-s2-integrated-r6。目标完整SHA以固定issue #12当前正文为准。包含r5/T56批量筹资，以及T57答案外层纠正、T58经济路径复用、T59三塔优先。替代R207；PK651598/PK651884实际buildId仍r4，不算r5验证。停止追补r4旧局，不再开r4/r5对战。

## 先核实实际部署，错版立即停止

拉取目标提交，核对完整git HEAD及工作区差异，不覆盖本地修改、不reset。从该提交打包，记录包SHA256、文件清单和构建标识；排除缓存、.DS_Store及凭据。确认平台实际上传并启用该包，先用平台允许的部署信息/健康检查核对版本。首场首个可取得的真实日志buildId必须为nightwatch-s2-integrated-r6，同时关联完整SHA与包hash；任一不符就停止后续对战，报告实际版本及部署链断点，不能继续跑两场再发现错包。buildId字符串单独不能替代文件与SHA核验。

## 场次与停止条件

最多2场，优先challenger，再defender；不能指定阵营则记录实际阵营，不刷额外场。首场如错版、协议/HTTP异常、持续无有效动作、明显无法回防或规划卡死，停止第二场并反馈。自然未覆盖标未覆盖，不制造物品/需求/错误，不修改策略或追加场次。原始日志、题面、答案、凭据留内网。

## 首要：三塔、首夜施工和炮手（#9/#11）

白天三塔未齐、现金或本工人可售矿、合法塔位及完整施工回防窗口具备时应优先补塔；普通旧采矿/投资与远途持券让位，即时Medicine和危险墙救援保留。保护真实金币，不假定未售矿到账。已有相邻修墙券优先兑现、避免重复买券或远处工人抢占。三塔齐后普通工作恢复。

记录R1–70每座塔build回合/目标/成功反馈、逐轮金币变化原因、关键工人背包和计划reason、fortification状态/skipReason、towerPriority、夜前角色位置/指定炮位/实际路径与首攻。若未造第三塔，定位首次资金>=25仍未造的回合，给资金占用、候选/拒绝原因和完整窗口证据，不只贴最终余额。两塔零墙时判断是资金、路线、角色占用还是旧计划遮挡，不从单一skipReason猜根因。return_deadline缺失不等于没回防。

分夜记录基地与炮手HP、死亡/离岗/攻击、墙缺口。昼夜130回合：夜1 R71–130，夜2 R201–260，夜3 R331–390，夜4 R461–520。R391零机器人不能证明提前清场；R382墙毁属第三夜。清场使用完整列表、targetTeam和truncated证据，不用16项摘要当总波次。夜间仍不得build。提前结束不当完整夜或S2通过。

## 任务收尾与失败证据（#10）

逐实例记录accept、命令次数/退出码、答案信封分类、submit实际反馈、奖励与结束原因。非法答案外层允许一次格式纠正，要求保留已有答案、仅answer/abandon；纠正后合法答案经正常仲裁提交，不增加命令或截止预算。仲裁暂拒时应保留deferred答案，不重复调用LLM。禁止把exit0、读取成功或本地completed当裁判成功。

自然遇到CRLF的bad interpreter、占位token或连续404时，给脱敏的失败命令结构/退出码/必要错误行、后续处理和提交分类，确认是否同一任务实例；完整题面/真实token/凭据留内网。这些语义/工程失败未在r6宣称修复，不因外层合法就标已解决，不自动修改沙箱或扩大工具预算。

## 筹资与性能（#9）

自然批量筹资按角色串联collect→move→sell→buy→use→回岗，列真实多个需求/初始批量目标/需求失效缩批、工人动作数与移动数、首次及第二次兑现回合。单次buy数量1不代表批量筹资未生效；没有多个可兑现需求时单买正常。核对不重复购同目标、不超花、不因追加采矿延误救援/建塔/回岗。

按昼夜记录decide/serverWriteComplete的p50/p95/max及>3800/>5000ms。慢段给pathSearches/pathComputations/pathCacheHits/pathExpansions、截止原因、墙/采购计划和实际动作。T58减少同图重复寻路；T59投影建塔地图必须隔离缓存，原4秒/3秒/1600预算不变。未超4秒不等于性能完全解决，不以不同局耗时直接量化优化收益。

## 反馈与结束

每场完整SHA、包hash、实际buildId、PK、阵营、对手、比分、日志范围与真实终局依据。#9经济性能，#10任务，#11防守：各写短增量，区分已核实/推导/证据不足/未覆盖，不跨局混用基地HP/升级记录、不三处复制全文。完成最多2场即停，不编辑或关闭#12，不自动关闭缺陷；无需旧局补证阻塞本批。
